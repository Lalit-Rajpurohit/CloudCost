# Docker-specific configuration overrides

# H2 Database - store in mounted volume
spring.datasource.url=jdbc:h2:file:/app/data/costs;AUTO_SERVER=TRUE

# Disable H2 console in Docker for security
spring.h2.console.enabled=false

# Logging
logging.level.root=INFO
logging.level.com.cloudmonitor=INFO
